---
tags: [topic, long-term-memory, memory-provider, context-injection, rag]
created: 2026-07-09
updated: 2026-07-16
---
# 장기기억 아키텍처 (LLM long-term memory)

`[[progdesigner]]`가 LLM 어시스턴트에게 **과거 작업 이력을 기억시키는 방법**을 정리한 주제.
2026-07-08 설계 세션([[2026-07-08-장기기억-provider-연동-설계]])에서 도출.

## 핵심 통찰: "저장 ≠ 조회"

- 사용자가 겪은 문제: "기억에 저장" 아이콘으로 저장은 되지만, 어시스턴트가 그 내용을 **다시 읽지 못함**.
- 진짜 병목은 저장이 아니라 **저장된 것이 컨텍스트로 재주입되거나 검색 가능한가**이다.
- 검증법: 고유 문장을 저장 → 새 대화에서 질문 → 답하면 주입 O, 모르면 주입 X.

## 3계층 저장 전략 (역할 분리)

| 계층 | 무엇을 담나 | 특징 |
|---|---|---|
| "기억에 저장" 아이콘 | 안 변하는 **선호·규칙** | 짧고 명확하게. 긴 이력 금지 |
| `[[john-wiki]]` | 실제 **장기 작업 이력·프로젝트 기록** | 길어도 됨. 검색 대상 |
| `AI_CONTEXT.md` | 매 대화 **자동 주입 요약** | john-wiki 루트, system/developer 메시지로 주입 |

**저장에 좋은 것**: 고정 선호(항상 한국어), 반복 프로젝트 배경, 약어·명칭, 장기 목표, 답변 포맷.
**넣지 말 것**: 임시 지시, 일회성 코드, 인증정보/키, 긴 회의록 전체, 자주 바뀌는 상태값.

## 조회를 붙이는 3가지 방법 (난이도순)

- **A. 요약 붙여넣기** — 대화 시작마다 john-wiki 요약을 직접 붙임. 단순·확실하나 수동.
- **B. `AI_CONTEXT.md` 자동 주입** — AI용 요약 페이지를 만들고 harness가 매번 주입. 추천.
- **C. 검색 tool 연결** — harness에 `search/read/list` tool을 붙여 LLM이 능동 검색. "진짜 장기기억"에 가장 근접. → [[wiki-memory-provider-integration]]

## 안티할루시네이션 규칙

조회 tool을 붙일 때 반드시 모델 지침에 포함:
1. 과거 작업/장기기억을 물으면 먼저 tool로 조회한다.
2. 검색 결과 기반으로만 답한다.
3. 조회 실패·tool 부재 시 **접근 불가라고 명시**한다.
4. **기억에 없는 작업 이력을 지어내지 않는다.**

## 구현 진행 상태 (2026-07-11~12 관찰)

2026-07-08 설계 이후의 상태 점검([[2026-07-11-기억-요약-wiki-경로-확인]]):

- **방법 C(검색 tool 연결)는 아직 미완** — `[[lampas-harness]]` `src/config.ts`에 `wikiDir`+`WIKI_DIR` 설정이 들어와 배선은 착수됐으나, 어시스턴트에 `search/read/list` tool이 노출되지 않아 채팅에서 "기억 요약"·wiki 위치 질문에 여전히 답 못 함. **같은 날 두 개의 독립 채팅에서 재확인됨**([[2026-07-11-기억-요약-wiki-경로-확인]] · [[2026-07-11-기억-저장내용-조회가능여부]]).
- **노출된 파일 tool은 위키가 아닌 코드 저장소로 스코프됨** — 후자 채팅에서 어시스턴트는 대안으로 `search_files`/`list_directory`를 제시했으나 그 범위는 작업 폴더(`lampas-harness`)일 뿐, `[[john-wiki]]` 코퍼스가 아니다. 파일 tool이 있어도 위키 경로에 연결돼야 방법 C가 성립한다.
- **경로 불일치 함정** — `wikiDir` 기본값 `~/Works/llm-wiki`가 실제 저장소 `~/Works/john-wiki`와 다름. `WIKI_DIR` env로 지정 안 하면 없는 경로를 가리킴.
- 교훈: 조회 tool 연결은 "config에 경로 하나 추가"로 끝나지 않는다 — tool 노출·주입·검증(→ [[wiki-memory-provider-integration]] 5~7단계)까지 가야 어시스턴트가 실제로 조회한다.
- **방법 B(AI_CONTEXT.md 주입)도 quick 채팅엔 아직 안 닿음 (2026-07-12 관찰)** — 2026-07-12자 `AI_CONTEXT.md` 상시 주입 결정(커밋 0c05aae) 이후, 같은 날 quick 채팅에서 "하네스가 뭐야"에 CWC·lampas 맥락 없는 **완전 일반 사전식 답변**이 나왔다([[2026-07-12-하네스-뜻-질문]]). quick-chat 경로가 `AI_CONTEXT.md`를 주입하지 않거나 배선 이전 발생 — 주입 배선이 모든 진입 경로(웹/데스크톱 퀵)에 일관 적용되는지 검증 필요.

## 저장(ingest) 측 자동화 — 보관→ingest (2026-07-13)

위 관찰이 주로 **조회(recall) 측** 배선이라면, 2026-07-13 세션은 **저장(ingest) 측**을 자동화했다
([[2026-07-13-보관시-자동-기억저장]]). `[[lampas-harness]]` 보관(🗄) 동작이 `memory-ingest` 잡을
자동 큐잉하게 되어, 대화를 보관하면 위키에 이력이 쌓인다.

- 이제 위키로의 저장 진입점이 **셋**: (1) 수동 `🧠 기억에 저장` 버튼, (2) 보관 🗄 자동, (3) 야간 자동
  ingest 잡. 세 경로 모두 같은 `memory-ingest` 잡을 큐잉하고 `rememberedAt`/source 키로 중복을 막는다.
- 즉 저장 계층(john-wiki에 이력 축적)은 상당히 자동화됨. 남은 병목은 여전히 **방법 C(능동 조회 tool)** —
  쌓인 이력을 어시스턴트가 채팅 중 검색해 읽는 부분.
- **확인 방법(2026-07-14 사용자 재확인 세션)**: 보관이 실제로 ingest 큐잉됐는지는 API 응답의
  `remembered: true` 값, 또는 데몬 로그의 `chat archived: <id> (memory ingest queued)` 문구로 확인
  가능. 보관은 **동기 저장이 아니라 잡 큐 enqueue**이므로, 위키 실반영은 `memory-ingest` 잡 처리 후
  이뤄진다 — 방금 보관했다면 로그·위키 커밋으로 완료를 별도 확인해야 한다. →
  [[2026-07-14-보관-자동저장-확인-볼린저밴드]]

## 코드 레벨 재확인 — 큐 경로·야간 잡 파일명 (2026-07-15, 세 번째 확인)

`[[2026-07-15-보관메모리확인-하네스재시작-커밋푸시]]` 세션에서 어시스턴트가 `src/server.ts`·
`src/queue.ts`·`src/daemon.ts`·`src/config.ts`를 직접 읽어 라인 단위로 재확인(2026-07-13 구현 →
2026-07-14 1차 재확인 → 이번이 2차 재확인). 새로 구체화된 사실:

- `enqueueJob`(`src/queue.ts:54-61`)은 `queue/`에 잡 파일을 쓰기만 하고 즉시 반환 — HTTP 응답도
  `{ ok: true, remembered: true }`로 바로 나감. **enqueue 성공 = 위키 반영 완료가 아니다**(비동기).
- `src/daemon.ts:39-95`가 `config.pollIntervalMs`(기본 5000ms, `src/config.ts:24`) 간격으로 큐를
  폴링, rename으로 원자적 선점 후 실행 — 최대 15분 타임아웃(`jobs/memory-ingest.job.json`).
- **야간 안전망 배치의 실제 파일명**: `jobs/memory-ingest-daily.job.json` +
  `io.lampas.harness.memory-ingest-daily.plist`. 이전엔 블로그 서술(아래 "사용자 공개 서술" 절)의
  "새벽 5:30 launchd 크론"이라는 뭉뚱그린 표현만 기록돼 있었는데, 이번에 정확한 잡/plist 이름이
  코드에서 확인됐다 — 주 1회 `memory-lint`(모순·고아 정리)와는 **별개 파일**임도 함께 확인.

## 사용자 공개 서술 vs 관찰 (2026-07-13 블로그)

`[[progdesigner]]`가 네이버 블로그 `study-ai-what`에 이 아키텍처를 **공개 연재**하며 서술한 내용
([[2026-07-13-블로그-ai기억글-해시태그]]). 이 위키의 관찰과 **부분적으로 어긋나므로** 병기한다:

- 블로그(사용자, 2026-07-13) 주장: 회수(recall)가 **3층으로 이미 동작** — (1) AI_CONTEXT.md 상시 주입,
  (2) index.md 기반 능동 조회로 **로컬 LLM(Rapid-MLX)이 `search_memory`/`read_memory`/`list_memory`
  읽기 전용 MCP 3도구**로 위키를 직접 검색, (3) **위키 corpus 전체를 로컬 모델에 넘겨 요약을 스트리밍**하는
  별도 API. 자동화는 **매일 새벽 5:30 launchd 크론** ingest + **주 1회 `memory-lint`**.
- 위 관찰(2026-07-11~12 채팅): **방법 C(능동 조회 tool)는 미완**으로 기록됨(위 "구현 진행 상태" 절).
- 해석: 블로그의 (2)·(3)은 **로컬 LLM 경로에 한정**한 구현이거나, 지향/설계의 서술일 수 있다. Claude
  채팅 경로에서 방법 C가 실제로 동작하는지는 **여전히 미검증** — 실측 재확인 필요. (5:30 크론·주간 lint는
  기존 "야간 자동 ingest" 서술을 시각·주기까지 구체화한 것.)

## 저장(ingest) 실패 모드 — API 크레딧 소진 (2026-07-15 관찰)

야간 자동 ingest·보관 자동 ingest 모두 **같은 `memory-ingest` 잡**을 사용하는데, 이 잡이 API 종량
과금 경로를 타는 경우(백그라운드 잡이라 사용자가 토글을 직접 못 켜므로, 어떤 조건에서 API 모드로
빠지는지는 이 세션 소스만으로는 불명) **Anthropic API 크레딧 부족 시 그대로 실패**한다 — 22:27에
2회 재시도 모두 "Credit balance is too low"로 실패한 사례 확인. 저장 진입점 자동화(위 "저장 측 자동화"
절)가 됐다고 해서 **항상 성공한다고 가정하면 안 된다** — 실패는 조용히 쌓일 수 있으므로, 이 위키의
최신성을 신뢰하려면 로그(`logs/daemon.*.log`)나 `remembered`/커밋 이력으로 주기적 확인이 필요하다.
→ [[sdk-claude-code-vs-api-billing]] · [[2026-07-15-gpt-realtime-음성입력-길게누르기]]

## 이 위키(john-wiki)와의 관계

이 저장소 자체가 이 아키텍처의 B/C 계층 구현체다. CLAUDE.md의 ingest/query 워크플로가
"조회 가능한 장기기억"을 사서(LLM) 주도로 유지하는 방식이며, `index.md`가 사실상 검색 진입점(A/B),
`raw/` + `wiki/`가 검색 대상 코퍼스(C) 역할을 한다.

## 관련
- 세션: [[2026-07-08-장기기억-provider-연동-설계]] · [[2026-07-13-보관시-자동-기억저장]] · [[2026-07-13-블로그-ai기억글-해시태그]] · [[2026-07-14-보관-자동저장-확인-볼린저밴드]] · [[2026-07-15-gpt-realtime-음성입력-길게누르기]] · [[2026-07-15-보관메모리확인-하네스재시작-커밋푸시]]
- 엔티티: [[john-wiki]] · [[lampas-harness]] · [[progdesigner]]
- 스킬: [[wiki-memory-provider-integration]] · [[sdk-claude-code-vs-api-billing]]
