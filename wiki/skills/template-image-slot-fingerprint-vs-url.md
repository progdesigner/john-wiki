---
name: template-image-slot-fingerprint-vs-url
description: 저장된 템플릿의 이미지가 특정 브라우저/기기에서만 안 붙을 때 fingerprint(로컬)/url(공개 CDN) 필드를 확인·보정하는 절차
created: 2026-09-25
updated: 2026-09-26
tags: [lampas-studio, lampas-web-edit, template, s3, cloudfront, cross-device]
---
# 템플릿 이미지 슬롯 fingerprint vs url

> **원출처 정정(2026-09-26 ingest)**: 이 스킬은 처음 2026-09-25 세션(계정별 데이터 땜질)을 출처로
> 기록했으나, `url` 필드와 이 폴백 로직 자체는 그보다 5~6일 앞선
> [[2026-09-19-lampas-edit-자막-템플릿-대량기능개발]] 세션(`[[lampas-web-edit]]` 0.1.42)에서
> **처음 구현**됐다. 그 세션에서 사용자가 SPOTV 템플릿 이미지 2장(로고)을 첨부하며 "S3 업로드 경로를
> 쓰도록 해달라"고 요청 → 로고를 S3(`cdn/production/edit-templates/logos/`)에 올리고, 템플릿 이미지
> 슬롯에 `url` 필드를 신설, **적용 시 우선순위를 "로컬 → 템플릿 `url` → 로그인 계정의 원본 자동
> 백업" 순으로 확장**했다. 09-25 세션은 이 기존 메커니즘을 이용해 `url`이 비어 있던 **다른 계정**의
> 템플릿 레코드만 땜질한 것 — 아래 절차는 그대로 유효하지만 "발견"이 아니라 "적용"이었다는 점을 정정.

## 언제 쓰는가
저장된 템플릿(또는 이와 유사한 "재사용 가능한 프리셋")을 적용했을 때, 이미지가 **만든 브라우저/기기에서는
보이는데 다른 브라우저·다른 로그인 세션에서는 안 보이는** 증상. 로컬 저장소(OPFS·IndexedDB 등)에 의존하는
에디터에서 전형적으로 재발한다.

## 절차 (단계별)
1. 증상을 보인 템플릿의 서버 저장 payload를 직접 확인한다(계정별로 동일 이름의 템플릿이 여러 개 있을 수
   있으니 **어느 계정 것이 실제로 문제인지** 특정한다).
2. 이미지 슬롯 스키마를 확인: 로컬 전용 키(예: `fingerprint`=브라우저 로컬 저장소 키)와 공개 URL 필드
   (예: `url`=S3/CDN 주소)가 분리되어 있는지 본다. 문제 템플릿은 보통 `url`이 비어 있고 `fingerprint`/
   파일명만 있다.
3. 정상 동작하는 동일 유형의 다른 템플릿(다른 계정 것 등)과 대조해 **`url` 필드가 채워진 정상 예시**를
   확보한다.
4. 사용자가 원본 이미지를 다시 준다면, 재업로드 전에 **기존 CDN 자산과 MD5를 대조**한다 — 이미 올라가 있는
   경우가 많아 불필요한 재업로드·CDN 무효화를 피할 수 있다. CDN 응답 200·CORS 헤더도 함께 확인.
5. 코드/배포 변경 없이 **서버 데이터만 수정**: 비어 있는 `url`에 기존(또는 새로) 업로드된 CDN 주소를
   채우고 파일명을 맞춘다. `fingerprint`는 지우지 않는다 — 원본 브라우저의 로컬 매칭을 계속 살려두기
   위해서다(로컬에 있으면 로컬 우선, 없으면 `url` 폴백이라는 기존 로직을 그대로 활용).
6. `updatedAt`(또는 동급 버전 필드)을 갱신해 다른 클라이언트가 다음에 열 때 서버 버전이 로컬 캐시를
   덮어쓰도록 한다.
7. 사용자에게 확인 방법을 구체적으로 안내한다 — 특히 "로그아웃 상태에서는 서버 템플릿 자체가 목록에
   안 뜬다"처럼 재현 전 필요한 선행 조건이 있으면 반드시 언급한다.

## 주의사항 / 함정
- 같은 이름의 템플릿이 **계정별로 별도 레코드**로 존재할 수 있다 — 증상을 보인 계정 것만 고쳐야 하며,
  다른 계정 템플릿을 잘못 건드리지 않는다.
- 근본 원인은 **드래그/붙여넣기로 이미지를 넣어 템플릿을 만드는 경로가 `url` 없이 저장**하는 것이다.
  이번 건은 데이터만 땜질했을 뿐 **재발 방지(생성 시점에 공개 URL을 자동으로 채우거나, 공용 자산은
  라이브러리에서 URL로 선택하게 강제하는 것)는 코드 변경이 필요**하며 아직 하지 않았다.
- 공용 로고·아이콘처럼 여러 템플릿에서 재사용되는 자산은 애초에 CDN 경로(`cdn/production/edit-templates/
  logos/` 류)에 올리고 URL로 라이브러리에 등록한 뒤 템플릿을 만들면 이 문제 자체가 발생하지 않는다.

## 폴백 3단계의 실제 인프라 출처 (2026-09-26 추가 ingest)
위 절차 1번 문단이 말하는 "로컬 → 템플릿 `url` → **로그인 계정의 원본 자동 백업**" 3단계 중 마지막
단계는 0.1.42(09-19)가 새로 만든 게 아니라, 그보다 6일 앞선
[[2026-09-13-lampas-edit-이미지트랙-텍스트효과-원본백업-구현]] 세션이 구축한 `EditSessionAsset`
fingerprint 기준 S3 백업 인프라를 그대로 가져다 쓴 것이다. 0.1.42는 그 기존 백업 조회를 템플릿
이미지 슬롯의 폴백 체인 마지막 단계로 **연결**만 했다. 일반화된 백업 절차 자체는
[[local-asset-fingerprint-s3-backup-recovery]] 참고.

## 출처: [[2026-09-19-lampas-edit-자막-템플릿-대량기능개발]](`url` 필드·폴백 순서 최초 구현) ·
[[2026-09-25-edit-템플릿-이미지-s3-url-수정]](다른 계정 레코드에 같은 메커니즘 적용) ·
[[2026-09-13-lampas-edit-이미지트랙-텍스트효과-원본백업-구현]](폴백 3단계 중 "원본 자동 백업" 인프라 원출처)
