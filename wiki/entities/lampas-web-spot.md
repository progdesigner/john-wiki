---
tags: [entity, project, product, lampas-studio, map, openstreetmap, kakao, geocoding, spot]
created: 2026-09-26
updated: 2026-09-26
---
# lampas-web-spot

`[[lampas-studio]]` 저장소(`lampas-system`) 내 앱 — **맛집(식당) 지도 서비스**, 배포 도메인
`spot.lampas.io`. Lampas 제품 라인 소속(다른 `lampas-web-*` 앱과 동일 배포 경로).

**2026-09-26 갱신 — 스텁 해소**: 이 앱을 실제로 만든 세션이 [[2026-09-24-spot-맛집지도-구축-지도전환-신고기능]]
(`Tool: codex`, 2026-09-24T00:42Z 시작~2026-09-25로 이어짐)로 확인·ingest됨. 아래는 해당 세션
기준 실제 구현 내용.

## 데이터
- 소스 1: 사용자 첨부 엑셀 → 블루리본 맛집 579곳(리본 등급별 분포 포함, 좌표 없음).
- 소스 2: 사용자 공유 구글 시트 → 블루리본 통합 657곳 + 다이닝코드 90점 이상 86곳, 중복 5건
  병합 → 최종 **738곳**. 출처(블루리본/다이닝코드)·동네(석촌호수·광화문·압구정·도산공원·군자동)
  필터 지원, 다이닝코드 항목엔 점수·영업시간·메뉴 추가 표시.
- 원본 사이트(`bluer.co.kr`)는 403으로 직접 스크래핑 불가 — 사람이 내려준 엑셀/시트가 유일한
  데이터 경로.
- 좌표는 [[nominatim-batch-geocode-progressive-rollout]] 절차로 주소→좌표 변환. 738곳 중
  **502곳만 지도 표시**(주소·건물번호 정확히 일치하는 것만 핀 처리), 236곳은 위치 미확인으로
  목록 + 카카오맵 검색 링크 유지.

## 지도 — **최종 OpenStreetMap 단일 경로** (모순 기록)
이 앱은 세션 진행 중 지도 프로바이더를 3번 바꿨다. **최종 결정은 OpenStreetMap(키 불필요)**이며,
카카오·네이버 지도는 모두 폐기됐다:
1. 카카오 지도 컨셉으로 UI 골격 먼저 구현(데이터 0건 상태).
2. 지도 위 핀 요구 → 카카오/네이버는 사이트 삽입에 키가 필요해 **OpenStreetMap + Nominatim**
   지오코딩으로 전환, 396핀까지 배포.
3. 사용자가 네이버 클라우드 Client ID를 제공해 네이버 지도로 재전환 시도 → `spot.lampas.io`에서
   **401 인증 실패**(Dynamic Map 미활성/Web 서비스 URL 미등록 추정), 인증 실패 시 OSM으로 자동
   폴백하는 코드만 배포된 상태로 유지.
4. 최종적으로 사용자가 "네이버 지도는 제거하고 오픈 맵으로" 지시 → 네이버 SDK·인증 오류 UI
   전부 제거, **OpenStreetMap 단일 경로로 확정**.

> **이전 기록과의 모순 해소**: 이 페이지가 처음 생성됐을 때(2026-09-26, 원격 병합 커밋
> `e07db82f` 기준) `.env.local`/`env/.env.production`에 **Naver 지도 클라이언트 ID**만 보여
> "네이버 지도 기반으로 추정"이라 기록했었다. 실제로는 그 env가 위 3번(네이버 재전환 시도) 중간
> 시점의 스냅샷이었을 뿐이고, 이 세션의 최종 상태는 4번(OSM 확정)이다. env 파일의 네이버 키가
> 이후 실제로 정리(제거)됐는지는 이 세션 소스만으로 확인 불가 — 다음 코드 조사에서 재확인 필요.

## 기능
- 검색, 지역/리본 필터, 출처(블루리본/다이닝코드) 필터, 동네 모음.
- 즐겨찾기(브라우저 저장), 카카오맵 길찾기 링크(지도 프로바이더와 무관하게 길찾기만 카카오
  경유 유지).
- **폐업/정보 신고**: 사유·설명 입력 → 접수 번호 표시 → 신고자 개인 지도/목록에서 즉시 숨김·
  복원 가능. 신고 내용은 신설 인프라 없이 **기존 Lampas 관리자 문의함**에 저장. 전체 목록 제외는
  운영자가 문의함을 검토한 뒤 수동 반영하며, 제외된 장소는 구글 시트 재동기화·개인 숨김 복원과
  무관하게 계속 제외 상태 유지.

## ⚠️ 모순 기록 (2026-09-26 추가) — "OSM 확정" 이틀 만에 카카오맵 재도입 준비
위 "최종 OpenStreetMap 단일 경로" 결정은 2026-09-24/25 세션 기준이다. 불과 이틀 뒤인
[[2026-09-26-카카오-지도-api-키-발급]] 세션에서 `[[progdesigner]]`가 다시 "카카오 지도 서비스를
연동하려고 해"라고 요청해 카카오 앱(ID **1588569**, 이름 "Lampas Spot")을 새로 만들고 JavaScript/
REST/네이티브 키 3종을 발급·도메인(`spot.lampas.io`, `localhost:8478`) 등록까지 완료했다. 키는
`apps/lampas-web-spot/env/.env.production`·`.env.local`·최상위 `.env.local` 3곳에 반영됐고
README에 "카카오맵 연동" 절도 추가됐다.

**단, 실제 지도 SDK 교체는 이 세션에서도 진행되지 않았다** — spot 앱은 여전히 Leaflet + OpenStreetMap을
쓰고 있고, 어시스턴트 스스로 "지도 SDK 교체 작업은 별도로 진행 필요"라고 남겼다. 즉 현재 상태는:

- **실제 렌더링 지도**: 여전히 OpenStreetMap (2026-09-24/25 확정 그대로, 미변경)
- **발급·설정 완료, 미사용 대기 중**: 카카오맵 JS/REST/네이티브 키 (2026-09-26 신규)

두 기록이 같은 앱을 두고 "OSM으로 확정"과 "카카오로 재전환 준비"라는 상반된 방향을 담고 있다 —
어느 쪽이 최종 상태인지는 **다음 세션에서 SDK 교체 착수 여부로 재확인 필요**. 카카오 키 발급 세션
자체는 REST API 키(서버 전용)를 env에 넣지 않고 JS 키(도메인 제한 클라이언트 키)만 넣은 판단을
남겨 [[secrets-plaintext-exposure-pattern]]에도 반영했다.

## 관련
- 상위 저장소: [[lampas-studio]] (`lampas-system`)
- 세션: [[2026-09-24-spot-맛집지도-구축-지도전환-신고기능]](실제 구현·OSM 확정 원본) ·
  [[2026-09-24-studio개선-seedance미니-노드툴바-멀티커밋푸시]](네이버 키가 남아있던 중간 시점을
  병합 커밋 메타데이터로 먼저 포착한 세션) ·
  [[2026-09-26-카카오-지도-api-키-발급]](카카오 키 재발급·모순 발생 세션)
- 스킬: [[nominatim-batch-geocode-progressive-rollout]]
